{
  "$schema": "https://turborepo.dev/schema.json",
  "tasks": {
    "build:source": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build:source"],
      "env": ["VISUALIZER"]
    },
    "build": {
      "dependsOn": ["build:source"]
    },
    "package": {
      "outputs": ["*.tgz"],
      "dependsOn": ["build"]
    },
    "build:package": {
      "dependsOn": ["build", "package"]
    }
  }
}
